# GuruFocus API — what actually works

Generated by `backend/scripts/gurufocus_catalog.py`. **Do not hand-edit** — re-run
the script. It caches every response, so a re-run costs zero API calls.

We pay for the **legacy** API: `https://api.gurufocus.com/public/user/{KEY}/<path>`.
It has **no official documentation**. The docs at `gurufocus.com/api/overview` and
`gurufocus.com/data-api/*` describe a *different, newer product* (plural paths like
`/stocks/{region_code}`) that our key does not authenticate against; the Excel
manual PDF documents the add-in, not the API.

Symbol forms: US listings take a bare ticker (`AAPL`); everything else takes
`EXCHANGE:TICKER` (`WBO:ANDR`) — see `ingest/earnings/_common.py::_build_symbol`.

## The trap: this API does not 404

`stock/{sym}/<anything>` returns **HTTP 200 and a 46-point series of zeros** for any
sub-path it doesn't recognise. Probed naively, `stock/AAPL/news` looks like a working
endpoint. Every probe below is compared against a canary sub-path that cannot exist;
anything matching it (or any all-zero series) is listed as a fallback, not an endpoint.

## The date filter: `?start_date=&end_date=` (undocumented, real)

`stock/{sym}/price` and `stock/{sym}/volume` accept a date range. Verified 2026-08-02
against `AAPL`, `XPAR:WLN` and `WBO:VER`, on both endpoints — the returned values are
identical to the unfiltered series, and a range returns exactly the bars in it.

| request | bars | bytes |
|---|---|---|
| `stock/AAPL/price` | 11,501 | 268,703 |
| `…/price?start_date=2026-07-31&end_date=2026-07-31` | 1 | **23** |

An 11,682× cut. It powers the daily gap fetch (`ingest/prices.py`) and the drift probe
(`ingest/history_drift.py`).

**⚠ Only this spelling filters.** `?from=&to=`, `?start=&end=`, `?date=`, `?limit=`,
`?last=`, `?period=`, `?range=`, `?days=` all return **HTTP 200 and the full series** —
no error, no hint. Send one of those and you get an 11,501-bar answer to a one-day
question: it parses fine and is wrong about what you asked.

**⚠ It does not save quota.** `api_usage` counts REQUESTS (`MONTHLY_API_LIMIT` =
20,000/region), so a one-day fetch costs the same single call as the whole history.
The win is time and bandwidth. When you need the full series, ask for it in ONE
unfiltered request — never walk it in windows, which multiplies the only scarce thing.

**⚠ `end_date` = today makes the API INVENT a bar.** Measured on Sunday 2026-08-02:

```
?start_date=2026-07-29&end_date=2026-07-31  -> 3 real bars
?start_date=2026-07-29&end_date=2026-08-02  -> the same 3, PLUS 2026-08-02 = 308.91
                                               (Friday's close, repeated)
?start_date=2026-08-01&end_date=2026-08-01  -> []        (a past non-trading day)
```

That extra row is the live-quote line, dated today, carrying the last close whenever
today has not settled. The **unfiltered** endpoint never does this — it stops at the
newest settled close — so a windowed fetch can manufacture phantom bars on weekends and
holidays that a full fetch would not. `ingest/prices.py::_settled_through` therefore
never asks beyond yesterday, and clamps to the real today so a caller's future-dated
`data_cutoff` can't reinstate it.

## Real endpoints (22)

| path | shape | note |
| --- | --- | --- |
| `cusip/037833100` | `list`[13] item keys: `exchange`, `symbol` | UNDOCUMENTED — a CUSIP sibling of isin/? |
| `exchange_list` | `dict`[98] keys: `Argentina`, `Australia`, `Austria`, `Bahrain`, `Bangladesh`, `Belgium` | UNDOCUMENTED — {region: [exchange codes]} |
| `insider_list` | `dict`[3] keys: `currentPage`, `data`, `lastPage` |  |
| `isin/AT0000730007` | `list`[13] item keys: `exchange`, `symbol` | UNDOCUMENTED — the Andritz case (WBO vs XPRA) |
| `isin/US0378331005` | `list`[19] item keys: `exchange`, `symbol` | UNDOCUMENTED — ISIN -> [{symbol, exchange}] for every listing |
| `stock/AAPL/analyst_estimate` | `dict`[2] keys: `annual`, `quarterly` | used by the earnings dashboard |
| `stock/AAPL/dividend` | `list`[91] item keys: `amount`, `currency`, `ex_date`, `pay_date`, `record_date`, `type` | PER-SHARE payments: amount, ex_date, record_date, pay_date, currency, type |
| `stock/AAPL/dividends` | `list`[46] | CASH FLOW for dividends ($ millions, negative) — NOT per share |
| `stock/AAPL/filings` | `dict`[2] keys: `data`, `total` |  |
| `stock/AAPL/financials` | `dict`[1] keys: `financials` | the 263-field blob; ~36,700 metric_data rows/company if parsed unrestricted |
| `stock/AAPL/gf_score` | `list`[248] |  |
| `stock/AAPL/gfvalue` | `list`[46] |  |
| `stock/AAPL/gurus` | `dict`[1] keys: `AAPL` |  |
| `stock/AAPL/insider` | `dict`[1] keys: `AAPL` |  |
| `stock/AAPL/keyratios` | `dict`[7] keys: `Basic`, `Dividends`, `Fundamental`, `Growth`, `Price`, `Profitability` |  |
| `stock/AAPL/ownership` | `dict`[7] keys: `Float_Percentage_of_TSO`, `Insider_Ownership`, `Institutional_Ownership`, `Share_Outstanding`, `company`, `display_symbol` |  |
| `stock/AAPL/price` | `list`[11485] |  |
| `stock/AAPL/quote` | `dict`[13] keys: `Currency`, `Current Price`, `Day's Change %`, `Day's Volume`, `Exchange`, `Price` |  |
| `stock/AAPL/summary` | `dict`[1] keys: `summary` | company_data incl. ISIN, currency, exchange, price — the reverse of isin/ |
| `stock/WBO:ANDR/dividends` | `list`[24] | non-US form ·  |
| `stock/WBO:ANDR/financials` | `dict`[1] keys: `financials` | non-US form ·  |
| `stock/WBO:ANDR/summary` | `dict`[1] keys: `summary` | non-US form ·  |

## NOT endpoints — router fallback (11)

These returned HTTP 200 with the canary's all-zero series. **Do not use them.**

| path |
| --- |
| `stock/AAPL/analyst_estimates` |
| `stock/AAPL/earnings` |
| `stock/AAPL/guru` |
| `stock/AAPL/indicator_price` |
| `stock/AAPL/news` |
| `stock/AAPL/profile` |
| `stock/AAPL/rank` |
| `stock/AAPL/segments` |
| `stock/AAPL/split` |
| `stock/AAPL/splits` |
| `stock/AAPL/valuation` |

## Real, but empty for this input (1)

The endpoint exists; the identifier we probed with didn't match a row.

| path | note |
| --- |  --- |
| `cusip/US0378331005` | an ISIN passed to cusip/ — expect an empty list |

## Missing (9)

404, non-JSON, or a transport failure.

| path | status |
| --- | --- |
| `country_list` | 404 |
| `economic_indicator_list` | 404 |
| `figi/US0378331005` | 404 |
| `guru_list` | 404 |
| `industry_list` | 404 |
| `search/AAPL` | 404 |
| `sector_list` | 404 |
| `stock_list` | 404 |
| `stock_list/USA` | 404 |

## Samples

### `cusip/037833100`

```json
[{"symbol": "AAPL", "exchange": "BSE"}, {"symbol": "APC", "exchange": "FRA"}]
```

### `exchange_list`

```json
{"USA": ["NAS", "NYSE", "OTCPK", "OTCBB", "AMEX", "ARCA", "IEXG", "BATS", "GREY"], "Canada": ["TSXV", "TSX", "XCNQ", "NEOE"], "UK": ["LSE", "LTS", "XPLU", "NEXX", "CHIX", "IPSX", "AQSE"], "Japan": ["TSE", "JAS", "OSE", "
```

### `insider_list`

```json
{"data": [{"cik": "0001181886", "name": "J Todd Mitchell", "address": "DEVON ENERGY CORPORATION, 20 N. BROADWAY, OKLAHOMA CITY OK 73102", "latest_transaction_date": "2011-03-01", "Companys": ["DVN"]}, {"cik": "0001177123
```

### `isin/AT0000730007`

```json
[{"symbol": "ANDR", "exchange": "WBO"}, {"symbol": "ANDR", "exchange": "XSWX"}]
```

### `isin/US0378331005`

```json
[{"symbol": "AAPL", "exchange": "WBO"}, {"symbol": "APC", "exchange": "XBUL"}]
```

### `stock/AAPL/analyst_estimate`

```json
{"annual": {"date": ["202609", "202709", "202809", "202909", "203009"], "revenue_estimate": [477599.6, 520531.73, 557206.37, 641364, 683200.5], "ebit_estimate": [155277.26, 167877.26, 184727.94, 202221.29, 217777.96], "e
```

### `stock/AAPL/dividend`

```json
[{"amount": "0.27", "ex_date": "2026-05-11", "record_date": "2026-05-11", "pay_date": "2026-05-14", "type": "Cash Div.", "currency": "USD"}, {"amount": "0.26", "ex_date": "2026-02-09", "record_date": "2026-02-09", "pay_d
```

### `stock/AAPL/dividends`

```json
[["1980-09-28", 0], ["1981-09-28", 0]]
```

### `stock/AAPL/filings`

```json
{"total": 82, "data": [{"form": "4", "descrpt": "4 - Apple Inc. (0000320193)", "display_symbol": "AAPL", "primary_document": "https://www.sec.gov/Archives/edgar/data/320193/000114036126025622/xslF345X06/form4.xml", "fili
```

### `stock/AAPL/financials`

```json
{"financials": {"financial_template_parameters": {"ind_template": "N", "REITs": "N", "IsDirect": "Indirect Method", "financial_report_frequency": "Quarterly"}, "annuals": {"Fiscal Year": ["1986-09", "1987-09", "1988-09",
```

### `stock/AAPL/gf_score`

```json
[["2006-01-01", 67], ["2006-02-01", 66]]
```

### `stock/AAPL/gfvalue`

```json
[["1980-09-28", 0], ["1981-09-28", 0]]
```

### `stock/AAPL/gurus`

```json
{"AAPL": {"picks": [{"guru": "Assenagon Asset Management S.A.", "date": "2026-06-30", "action": "Reduce", "impact": "-0.44", "price_min": "253.5", "price_max": "315.2", "Avg": "285.52", "comment": "Reduce -6.85%", "curre
```

### `stock/AAPL/insider`

```json
{"AAPL": [{"position": "Principal Accounting Officer", "date": "2026-06-16", "type": "S", "trans_share": "116", "final_share": "38,713", "price": "295.14", "cost": "34.2", "insider": "Borders Ben", "change": "7.14"}, {"p
```

### `stock/AAPL/keyratios`

```json
{"Basic": {"Price Updated Time": "2026-07-09 23:08:44 (EDT)", "Company": "Apple Inc"}, "Fundamental": {"Currency": "USD", "Reporting Currency": "USD", "Total Assets (Current)": "371082.000", "Book Value per Share": "7.26
```

### `stock/AAPL/ownership`

```json
{"company": "Apple Inc", "display_symbol": "AAPL", "exchange": "NAS", "Share_Outstanding": {"value": "14687.36(Mil)"}, "Institutional_Ownership": {"percentage": "73.56%", "value": "10803.83(Mil)"}, "Insider_Ownership": {
```

### `stock/AAPL/price`

```json
[["12-12-1980", 0.12835], ["12-15-1980", 0.121653]]
```

### `stock/AAPL/quote`

```json
{"timestamp": 1783652924, "Price Updated Time": "2026-07-09 23:08:44 (EDT)", "Current Price": 316.22, "Day's Change %": 0.88, "open": "310.510000", "Day's Volume": 47863060, "low": 308.16, "high": 316.53, "Currency": "US
```

### `stock/AAPL/summary`

```json
{"summary": {"general": {"company": "Apple Inc", "price": 316.22, "timestamp": "1783652924", "gf_score": "95", "rank_financial_strength": "5", "rank_profitability": "10", "rank_gf_value": "6", "rank_growth": "10", "rank_
```

### `stock/WBO:ANDR/dividends`

```json
[["2002-12-28", -12.02], ["2003-12-28", -12.2]]
```

### `stock/WBO:ANDR/financials`

```json
{"financials": {"financial_template_parameters": {"ind_template": "N", "REITs": "N", "IsDirect": "Indirect Method", "financial_report_frequency": "Quarterly"}, "annuals": {"Fiscal Year": ["2002-12", "2003-12", "2004-12",
```

### `stock/WBO:ANDR/summary`

```json
{"summary": {"general": {"company": "Andritz AG", "price": 72.7, "timestamp": "1783573200", "gf_score": "80", "rank_financial_strength": "8", "rank_profitability": "6", "rank_gf_value": "5", "rank_growth": "6", "rank_mom
```
